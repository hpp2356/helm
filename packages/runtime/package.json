{
  "name": "@helm/runtime",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "tsc --build"
  },
  "dependencies": {
    "@helm/core": "workspace:*"
  }
}
